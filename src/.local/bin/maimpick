#!/bin/sh

output=$HOME/Pictures/"$(date '+%Y-%m-%d_%H%M%S').png"

case "$(printf "Copy\\nSave" | dmenu -i)" in
	"Copy") maim -s | xclip -selection clipboard -t image/png ;;
	"Save") maim -s $output ;;
esac
